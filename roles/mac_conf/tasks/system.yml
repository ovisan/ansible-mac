---

- name: Copy .vimrc
  get_url:
    url: https://raw.github.com/ovisan/dotfiles/master/home/.vimrc
    dest: ~/.vimrc

- name: Copy .config
  copy:
    src: files/.config
    dest: ~/
    directory_mode:
